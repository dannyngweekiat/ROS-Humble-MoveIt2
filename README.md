# ROS Humble MoveIt2 Docker Image

ROS Humble MoveIt2 docker image is a ROS2 docker image with MoveIt2 installed.

Build the image using the command below

```bash
docker build . -t ros-humble-moveit2
```
